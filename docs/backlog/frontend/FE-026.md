# FE-026 — Privacy Policy + Terms of Service Pages

**Epic:** PUBLIC  
**Priority:** P0  
**Status:** DONE — implemented in merged PR #21 (verified 2026-09-17)  
**Parallel-safe:** YES — no dependencies beyond FE-003 (router defines these routes)  

---

## Product Requirement

Privacy Policy and Terms of Service pages are PRD P0 requirements. They are linked from the landing page footer, settings, and the Gmail OAuth consent explanation. Without them, the product cannot launch.

## Current State

`src/pages/PrivacyPolicyPage.tsx` and `src/pages/TermsPage.tsx` are placeholders from FE-003. Routes `/privacy` and `/terms` exist.

## Target State

- Both pages display the actual legal text
- Pages are accessible without authentication
- Accessible from landing page footer and settings
- Translated in EN and ES (minimum: the page structure and navigation — actual legal text may be English-only initially with a note)

## Scope

**Implement `src/pages/PrivacyPolicyPage.tsx`:**
- Public page (no auth required)
- Navigation: back link or logo
- Heading: "Privacy Policy" / "Política de Privacidad"
- Last updated date
- Legal copy (provided by product team — use placeholder text if not yet provided)
- Key sections translated: Introduction, What we collect, How we use it, Gmail access, Data deletion, Contact

**Implement `src/pages/TermsPage.tsx`:**
- Same structure as Privacy Policy
- Heading: "Terms of Service" / "Términos de Servicio"

**For both pages:**
- Render as static content
- Mobile-readable: max-width content column, adequate line-height
- No authentication required

## Out of Scope

- Cookie consent banner (post-launch)
- GDPR data processing agreements
- Legal content writing (product team provides content)

## Dependencies

- FE-003 (router defines `/privacy` and `/terms` as public routes)

## Acceptance Criteria

- [ ] `npm run build` succeeds
- [ ] `npm run lint` passes
- [ ] `/privacy` renders without authentication
- [ ] `/terms` renders without authentication
- [ ] Both pages are linked from landing page footer
- [ ] Both pages are linked from settings Privacy section
- [ ] Page headings translated in EN and ES
- [ ] Content is readable on mobile (375px)

## Test Requirements

- **Manual:** Visit `/privacy` and `/terms` without authentication — both render

## Verification Commands

```bash
npm run build
npm run lint
npm run preview
```

## Likely Files

```
src/pages/PrivacyPolicyPage.tsx         (implement)
src/pages/TermsPage.tsx                 (implement)
src/i18n/locales/en/public.json         (page headings, navigation)
src/i18n/locales/es/public.json         (page headings, navigation)
```

## Agent Notes

- Use placeholder legal text if the actual content is not yet written. The page structure and navigation must work; the legal content can be updated separately.
- These are static pages. Do NOT add any API calls or dynamic data.
- Use a simple `<article>` layout with `prose` Tailwind class pattern for readability.
